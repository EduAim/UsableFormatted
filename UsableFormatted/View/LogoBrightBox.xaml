<UserControl x:Class="UsableFormatted.View.LogoBrightBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10,10,0,0" MouseDown="Logo_MouseDown">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="22" />
            <RowDefinition Height="22" />
        </Grid.RowDefinitions>
        <TextBlock Text="USABLE" Foreground="#ffe7cf85" FontSize="20" FontWeight="Bold" Padding="0" Style="{DynamicResource DpTextBlock}" />
        <TextBlock Grid.Column="1" Text="&amp;" Foreground="#ffe7cf85" FontSize="20" FontWeight="Bold" Padding="0" Style="{DynamicResource DpTextBlock}" />
        <TextBlock Grid.Column="2" Text="&#xae;" Foreground="#ff235b8e" FontSize="20" Padding="0" Style="{DynamicResource DpTextBlock}" />
        <TextBlock Grid.Row="1" Grid.ColumnSpan="3" Text="FORMATTED" Foreground="#ff235b8e" FontSize="20" FontWeight="Bold" Padding="0" Style="{DynamicResource DpTextBlock}" />
    </Grid>
</UserControl>